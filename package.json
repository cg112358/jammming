{
  "name": "jammming",
  "private": true,
  "version": "0.0.0",
  "type": "module",

  "_note": [
    "Dev server bound to 127.0.0.1 instead of localhost because",
    "Spotify’s redirect URI validation (April 2025 update) disallows non-HTTPS localhost.",
    "Keep using 'vite --host 127.0.0.1' so login redirect matches Spotify’s settings."
  ],

  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "lint": "eslint .",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.36.0",
    "@types/react": "^19.1.16",
    "@types/react-dom": "^19.1.9",
    "@vitejs/plugin-react": "^5.0.4",
    "eslint": "^9.36.0",
    "eslint-plugin-react-hooks": "^5.2.0",
    "eslint-plugin-react-refresh": "^0.4.22",
    "globals": "^16.4.0",
    "vite": "^7.1.7"
  }
}

